import { CantidadValidaDirective } from './cantidad-valida.directive';

describe('CantidadValidaDirective', () => {
  it('should create an instance', () => {
    const directive = new CantidadValidaDirective();
    expect(directive).toBeTruthy();
  });
});
